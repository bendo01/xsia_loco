use sea_orm::entity::prelude::*;

use super::_entities::final_assignment_decrees::ActiveModel;

impl ActiveModelBehavior for ActiveModel {
    // extend activemodel below (keep comment for generators)
}
